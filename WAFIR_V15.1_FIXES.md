# WAFIR V15.1 — إصلاحات نسخة التشغيل

## إصلاحات رئيسية
- إصلاح خطأ `sell_price` في تقرير المخزون المنخفض إلى `selling_price`.
- منع انهيار الانتقال بعد نجاح تسجيل الدخول إذا فشلت تهيئة نافذة النظام؛ تبقى شاشة الدخول ظاهرة مع رسالة واضحة.
- حفظ `lot_number` و`expiry_date` القادمتين مع فاتورة الشراء في الدفعة المخزنية.
- ربط دفعات الشراء بالفرع الحالي.
- دعم دفعات المخزون القديمة غير المرتبطة بفرع عبر migration v6.
- جعل FEFO حساسًا للفرع مع الحفاظ على توافق الدفعات القديمة/المشتركة.
- إنشاء دفعة افتتاحية للمخزون التجريبي حتى يكون المخزون المعروض قابلًا للبيع عبر FEFO.
- تثبيت بيانات المبيعات التجريبية لتوليد 10 فواتير بنجاح.
- إضافة تحقق من الكميات والأسعار والخصومات والمدفوعات السالبة.
- إضافة اختبارات تغطي تسجيل الدخول، تقرير المخزون المنخفض، الدفعات، وبيع المخزون الافتتاحي.

## التحقق
- `compileall`: ناجح.
- اختبارات المشروع: **59 passed** في بيئة الفحص الحالية باستخدام shim مؤقت لـ `bcrypt` فقط لأن حزمة `bcrypt` لم تكن مثبتة محليًا. لم يتم تضمين الـ shim في المشروع.
- فحص `git diff --check`: ناجح.

## ملاحظة Windows
ملف `requirements.txt` ما زال يثبت `bcrypt==4.2.1`، وبالتالي يجب تنفيذ `pip install -r requirements.txt` في بيئة Windows كما في دليل البناء.
